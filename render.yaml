services:
  - type: web
    name: snake-online-server
    env: node
    rootDir: gametest/server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
